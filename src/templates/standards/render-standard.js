const standardTemplate = require("../_templates")["standard.html"];

/** @module renderPage
 * @param {Object}  standard  the standard object to render
 */
const renderStandard = standardTemplate;

module.exports = {
  renderStandard,
};
